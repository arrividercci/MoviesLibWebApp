@model MoviesWebAspNetMVC.Models.Movie

@{
    ViewData["Title"] = "Delete";
}

<h1>Видалити</h1>

<h3>Ви впевнені, що хочете видалити?</h3>
<div>
    <h4>Фільм</h4>
    <hr />
    <div class="card" style="width: 18rem;">
        <img src="/images/@Model.PicturePath" class="card-img-top" alt="image">
        <div class="card-body">
            <h5 class="card-title">@Model.Name</h5>
            <p class="card-text">@Model.Description</p>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item">@Html.DisplayNameFor(model => model.Author): @Model.Author.FullName()</li>
            <li class="list-group-item">@Html.DisplayNameFor(model => model.Genre): @Model.Genre.Name</li>
            <li class="list-group-item">@Html.DisplayNameFor(model => model.Country): @Model.Country.Name</li>
        </ul>
        <div class="card-body">
            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="Видалити" class="btn btn-danger" />
            </form>
        </div>
    </div>
    <a asp-action="Index" class="btn">Повернутись на головну</a>
</div>
